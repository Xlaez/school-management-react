import { createMuiTheme } from "@mui/material";
const theme = createMuiTheme({
    palette: {
        primary: {
            main: "#764abc"
        }
    },
    background: {
        default: "#fff"
    }
})

export default theme;